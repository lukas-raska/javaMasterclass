package dev.lpa.section5_fundamentals.codingExercises._1;

public class PositiveNegativeZero {

    public static void checkNumber(int number) {
        System.out.println((number == 0) ? "zero" : (number > 0) ? "positive" : "negative");
    }
}
